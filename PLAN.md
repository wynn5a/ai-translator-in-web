# 翻译体验与正确性改进计划

> 基于 2026-09 对当前代码（v1.3.0）的逐文件审读。每一条都标注了证据位置、
> 改法和验收方式。
>
> **排序原则：快是第一位，准确第二。** 并且准确手段（验收、重发）尽量设计成
> 「译文先上屏、校验在后台追加」，不为准确性增加可感知的等待。
>
> **范围前提：目标语言固定为中文。** 现有按多目标语言设计的分支据此裁剪：
> 语言漂移验收（`drifted()` 只对中文生效）恰好覆盖全部情况，无需泛化；
> 反而多出一批可简化的死分支（见第 3 条）。
>
> 两条全计划通用的约束：
> 1. 任何改动提示词的条目都必须把 `background.js` 里的 `PROMPT_VERSION` +1，否则旧缓存照常命中。
> 2. system 提示词必须保持逐字节静态（前缀缓存），页面可控文本只准待在 user 的参考资料块——所有新提示词都不得破坏这两条（见 background.js 「提示词」一节注释）。

---

## P0 速度

### 1. 长段落分片并行化（最大的一个延迟乘数）

- **证据**：`background.js` 的 `translateChunks()` 是串行 `for` 循环：每个分片要等前一片的**译文结尾**（`carry`）才发。一个 7000 字段落切 5 片，总延迟 ≈ 5 × 单片延迟，而单片延迟（网络 RTT + 生成）是大头——这是当前实现里唯一一个延迟随长度线性相乘的点。
- **改法**：分片改为**有界并行**（并发 3，复用 `MAX_CONCURRENT` 的名额机制），按序上屏（先完成的分片不能先画，否则段落顺序错乱——每片完成时把结果放进槽位，前面的槽位齐了才 `onChunk` 拼接）。连贯性用两个不占等待时间的手段补偿：
  - 每片在 user 参考资料块里带上**相邻分片的原文首尾**（前一片的结尾 200 字 + 后一片的开头 200 字，只读参考），替代现在「等前一片译文」的衔接方式——原文在手，不用等任何人；
  - 术语统一本就靠术语表注入（`glossaryFor` 已按分片快照），并行不破坏。
  - 全局加一个「连贯优先（串行）」开关给长难句场景，默认速度优先。
  - 与 P3 第 13 条「成稿通读」配套：并行换来的时间，可以花在一次可选的全文校对上。
- **验收**：单测改 `translateChunks` 的调度断言：分片并发不超过上限、上屏顺序与原文一致、某片失败不影响其余分片上屏与整体报错；长段落手测对比改前改后的完成时间。

### 2. 热键按下即预启动（省掉整段按住时长）

- **证据**：`content.js` 里 `keydown` 只做 `armedAt = performance.now()` 和高亮预览，真正 `trigger()` 在 `keyup` 之后。用户「按一下 Ctrl」的按住时长通常 100–400ms，这段时间里选区和目标段落早已确定，却什么都没发。
- **改法**：`keydown` 时就做两件事：快照当前选区/段落（轻量，同步）、对**划词翻译**直接预发请求（`runSelection` 的任务提前建立，`keyup` 时只做确认与 UI 收尾）；对**段落翻译**保守一些——先同步完成 `encodeInline` 编码，`keyup` 确认后再发。按住超过 `HOLD_MAX` 或中途按下其他键 → 中止预发任务（现有 `task.cancel()` 直接可用）。误触发风险与现状相同：keyup 判定逻辑原样保留，只是请求提前。
- **验收**：手测：按住超过 1.2 秒不触发（预发被中止，无请求计费或立即中止）；`Ctrl+C` 组合键不触发；正常轻按的完成时间对比。

### 3. 删掉触发路径上的异步语种检测

- **证据**：`content.js` 的 `alreadyTarget()`：目标语言固定为中文后，`TARGETS` 十条映射里九条是死代码；拉丁分支的 `chrome.i18n.detectLanguage` 异步调用（原本为了细分英/法/德）对「判断是不是中文」毫无必要——拉丁文字永远不是中文。这条异步调用卡在每次触发翻译之前，是纯浪费的延迟。
- **改法**：`alreadyTarget()` 简化为「`detectScript(text) === 'zh'` 即跳过」（日/韩排在中文之前的既有顺序保留：假名占比过阈值的日文不会误判成中文）；删除 `TARGETS` 映射、拉丁分支与 `chrome.i18n.detectLanguage` 调用，触发路径上不再有异步检测。`drifted()` 去掉 targetLang 参数。profile 的「目标语言」字段暂不删（存储结构不动），设置页隐藏该输入项。
- **验收**：单测改中文目标口径：中文段落 → 跳过；日文（含汉字）→ 不跳过；英文/俄文 → 不跳过；混合拿不准 → 不跳过（宁多翻不误跳）。README 的多目标语言说明同步改口径。

### 4. 缩短首字节前的提示词

- **证据**：每次请求的 user 参考资料块带着整页大纲（≤500 字）、页面简介（200 字）、相邻段落（3 × 300 字）、术语（≤12 条）。前缀缓存能折扣这些内容的**费用**，但更长的 prompt 仍直接增加首 token 的**延迟**（prefill 时间），尤其本地模型和非缓存首跳。
- **改法**：按任务类型裁剪注入内容：**查词**只带所在句 + 站点（义项判断不需要整页大纲）；**划词**去掉相邻段落，保留页面背景 + 术语；只有**段落翻译**保留全部。大纲按需缩短（一级 + 当前标题所在的二级，而不是全部标题）。
- **验收**：单测断言三类任务各自的注入内容清单；实测首字节时间对比（同一段落，改前/改后）。

---

## P1 准确（全部设计为不占等待时间：译文先上屏，校验后台追加，只在出错时多花一次重发）

### 5. 数字与信息完整性机械验收

- **证据**：`RULES` 第 2 条要求「数字、日期、单位原样保留」，但译完后没有任何检查；模型漏译半句、省略数字，目前会原样上屏并进缓存。
- **改法**：零成本确定性验收 `omissions(source, out)`，仅对 `text`/`block`：
  - **数字保真**：源文里每一串数字（含小数/千分位）都应出现在译文里——中文译文保留阿拉伯数字，此条无条件启用；
  - **长度比**：`stripMarkers(out).length < stripMarkers(source).length × 0.35` 判疑似漏译（英文→中文按字数通常缩到 0.25–0.5，阈值取实测下界，宁可少报不误报）。
  - 命中且未重发过 → 走现成单次重发通道（新增 `omitFix`，与 `langFix`/`repair` 同构，温度 0）。**首稿照常显示**，重发结果更好才替换；重发仍不过就维持首稿。不阻塞、不白等。
- **验收**：单测：漏一段的输出触发重发、数字齐全的正常译文不触发、数字多的技术文不误报。

### 6. 查词的句子边界被缩写切断

- **证据**：`content.js` 的 `sentenceAround()` 用 `[.!?。！？](\s|$)` 找句尾——`e.g.`、`Dr.`、`v1.2.`、小数点后跟空格都会把「所在句」拦腰截断，模型拿到半句语境，义项选错的直接原因之一。
- **改法**：句尾判定加两条否决：句点后第一个非空白字符是小写字母或数字则不断句；句点前是常见缩写（`e.g`、`i.e`、`etc`、`Dr`、`Mr`、`vs`、`No`、`Fig` 等常量清单）则不断句。判定提为纯函数。
- **验收**：单测覆盖 `e.g.`、小数、正常句末、分号。

### 7. 术语抽取可能把错误译法写进表并立即全局生效

- **证据**：`remember()` 后写覆盖先写，无冲突检查；`extractTerms` 抽出的译法在下一段就注入。某段译错一个术语会立刻污染整站后续段落——错误被术语表放大。
- **改法（轻量）**：同一条目重复记录且译法不一致时，保留**计数更多**的一侧（Map 值改 `{ target, hits }`）；相同译法命中时 `hits++`。旧格式读取时视为 `hits: 1`。
- **验收**：单测：同词异译保留高频侧；首次记录不受影响。

---

## P2 体验

### 8. 气泡与段落译文加「复制」按钮

- **改法**：气泡 bar 加「复制」（`navigator.clipboard.writeText`，成功变「已复制」1.5s）；段落译文节点尾部加同款小按钮（hover 显形）。复制内容用 `stripMarkers` 后的纯文本。
- **验收**：`tools/test.html` 手测 + Jira 这类 `!important` 站点确认按钮可点、不破坏样式。

### 9. 「强制重译」（绕过缓存）

- **证据**：重试按钮只在出错时出现；译文成功后觉得译得不好，没有办法让它重新译——缓存会原样返回。
- **改法**：气泡 bar 与段落译文尾部加「重译」：job 带 `force: true`，`translateWithContext` 命中缓存时跳过读取直接请求，成功后覆写缓存。后台一处改动，两处 UI 复用。
- **验收**：单测：`force` 时缓存命中也发请求、成功后覆写。

### 10. 超时可配置，本地模型自动放宽

- **证据**：`TIMEOUT = 30000` 写死。本地 Ollama/vLLM 首字节经常超 30 秒，表现是每次都「超时，请重试」。
- **改法**：`baseUrl` 命中 `localhost|127.0.0.1|ollama|vllm|lmstudio` 时放宽到 120s；设置页加可选「超时秒数」profile 字段（`PROFILE_FIELDS` 加 `timeout`，空值走默认逻辑）。
- **验收**：单测：本地端点解析 120s；配置了 timeout 的 profile 用配置值。

### 11. 分片进度提示

- **证据**：超长段落并行化（第 1 条）后仍是多次请求，页面只有加载点动画，用户不知道进展。
- **改法**：译文节点左上角小字显示「x/y」，随分片完成递增，译完消失。走旁路消息，不干扰流式文本通道与押稿逻辑（`gateDraft`）。
- **验收**：手测长段落：进度出现、递增、完成后消失。

### 12. 发音失败回退到 `speechSynthesis`

- **证据**：README 已知限制——Google TTS 非官方端点，会 429、会改版。
- **改法**：`speak()` 遇 429/网络错误时回 `{ fallback: true }`，喇叭按钮改用 `speechSynthesis`（宿主页 CSP 管不到它）。质量差一点但比「按钮变红」强。
- **验收**：mock 429 手测；GitHub（media-src 受限）上回退可用。

---

## P3 质量基建与更大的特性

### 13. 分片长段落可选的「成稿通读」

- **动机**：第 1 条并行化后，跨分片的长距离指代失去串行衔接的保障。
- **改法**：全部分片完成后可选发一次校对（默认关）：全文原稿 + 初稿，只允许输出修正后的全文。**初稿先上屏，校对是增量等待**，符合「快第一」；作为速度换连贯的补偿旋钮，与第 1 条的「连贯优先（串行）」开关二选一即可满足大多数场景。新任务类型 `polish`，独立缓存。
- **验收**：单测：默认关闭不发起；开启时初稿先交付、校对结果替换。

### 14. 提示词与后处理的离线评测集

- **改法**：`tools/` 下建 10–20 个固定段落的评测集（标记往返、术语消歧 `pitch` 三义、长难句、中英混排、代码保护），配「人工确认的期望译文要点」清单（中文译文按要点核对）。`tools/eval.js` 真实调 API 输出中文译文，人工打勾；`npm run eval`，改提示词前后各跑一次。不进 CI。
- **验收**：脚本有 `--dry-run`（只打印 prompt 不发请求）。

### 15. 整页翻译模式

- **改法**：右键菜单加「翻译此页」；content.js 收集可见正文块（复用 `BLOCKS` + 已译跳过），按 DOM 顺序并发 ≤3 调 `runBlock`，滚动到未译区域懒触发；工具栏给「停止」入口。第 1 条的并行调度经验直接复用。
- **验收**：手测长文与 SPA 页面；费用提示先确认再发。

### 16. 术语表管理界面

- **改法**：设置页按「站点 + 目标语言」列出术语（原文、译法、hits），支持单条删除与编辑（配合第 7 条尤其重要）；编辑走 `remember`/删除接口。
- **验收**：手测增删改；存储写回后翻译立即生效。

---

## 建议的实施顺序

| 阶段 | 条目 | 理由 |
| --- | --- | --- |
| 第一批 | 3、2 | 触发路径提速：删异步检测 + 预启动，改动小、风险低、每次翻译都受益 |
| 第二批 | 1 | 分片并行化，长段落延迟的最大乘数；需要调度单测兜底 |
| 第三批 | 4、5、6 | 首字节裁剪 + 两个不占等待时间的验收，纯函数好测 |
| 第四批 | 7、9、10、8、11、12 | 小改动打包一批，互不依赖 |
| 之后 | 14 → 13 → 16 → 15 | 先有评测集再动大特性；15 体量最大放最后 |

另外一条零代码的建议：**快慢主要取决于模型本身**。速度优先的场景，profile 里选低延迟模型（flash / mini / deepseek-chat 一档），把强模型留给第二套 profile 啃长难句——切换即生效，这个扩展已经支持了。

## 明确不做的

- **不改标记协议**（`<b1>/<n1/>/<t1>/<x1/>`）：它是现有正确性的基石，改协议要同时动编码、归一、分片、测试四层，收益不成比例。
- **不把页面可控文本挪进 system**：哪怕为了更好的上下文，提示词注入的约束不让步。
- **不为准确性牺牲首屏**：所有校验类手段（漂移、漏译、标记补发）都保持「先显示、后修正」的既有形态，第 5 条沿用同一纪律。
